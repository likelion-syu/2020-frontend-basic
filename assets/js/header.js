// HEADER에 연관된 함수들 
const _header = {
    // UI 조작에 관련된 함수들 
    ui : {
        // 남은 할일 표기
        getRemains : data => {
            
        },
        // 현재 날짜 표기
        setDate : ()=>{
            
        }
    },
    // 일반 함수들
    init : data =>{
        
    }
}